тип переменной в классе Box определяется конструктором, передаваемой в качестве параметра.
Ошибка в строке
Box<String> sample2 = new Box<>(1);
возникает из за несоответствия типов
Box - тип string, а
перетаваемый параметр new Box<>(1) - тип Integer;

правильно :
Box<Integer> sample2 = new Box<>(1);